global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: bookii-dev
    metrics_path: /actuator/prometheus
    static_configs:
      # DEV app currently runs as a host JAR, not as a Compose service.
      - targets: ["host.docker.internal:9091"]
